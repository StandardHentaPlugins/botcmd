{
  "main": "lib/index.js",
  "version": "2.2.0",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "release-it": "yarn version --patch && git push --follow-tags"
  },
  "dependencies": {
    "emittery": "^0.7.2",
    "readdirp": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^13.13.0"
  }
}
